const open = document.getElementById('open')
const close = document.getElementById('close')
const container = document.querySelectorAll('.container')

open.addEventListener('click', () => addEventListener.classList.add('show nav'))
close.addEventListener('click', () => addEventListener.classList.add('show nav'))
